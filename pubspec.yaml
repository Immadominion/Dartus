name: dartus
description: Dart implementation of the Walrus blob storage SDK with API parity to the Swift version.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.9.2 <4.0.0"

dependencies:
  crypto: ^3.0.6
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.2
